<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>test site </title>
  <style>
    *{margin:0; padding:0;}
    body{display:flex; justify-content:center; align-items: center;
          height: 100vh; background-image: url("#"); background-position: center; 
          background-repeat: no-repeat; background-size: cover;}
    .card{background-color: transparent; backdrop-filter: blur(10px);
          box-shadow:0 4px 8px rgba(53, 58, 95, 0.3); border-radius: 20px;
          padding: 30px 50px; height: 70vh; width: 40vh; gap: 20px;
          display: flex; align-items: center; flex-direction: column;}
    .prod{ display: flex; justify-content: center; align-items: center;}
    .prod img{ box-shadow: 0 4px 8px rgba( 0, 0, 0, 0.3); height: 100%; width: 100%; border-radius: 20px;}
    .desc{ display: flex; align-items: center; justify-content: center; flex-direction: column;
          width:100%; height: 60%; padding: 20px 5px; gap: 50px; color: #fff;}
    .desc h1{ margin-top: -40px;}
    .desc p{ margin: 10px;}
    .cta button{ padding: 0 50px; border-radius: 20px; border: solid trasparent 1px;
          font-family:sans-serif; font-size: large; background: linear-gradient(to right, #353a5f, #93bafe);
          background-repeat: no-repeat; color:#fff;}
</head>

<body>

  <div class="card">
    <section class="prod">
      <img src="#" alt="photo">
    </section>

    <section class="desc">
      <h1> Nome Prodotto </h1>
      <p>Lorem ipsum......</p>
    </section>

    <section class="cta">
      <button> Push </button>
    </section>

</div>

</body>

</html>

